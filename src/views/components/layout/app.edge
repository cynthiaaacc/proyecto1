<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{ title || "Your default title" }}</title>
        <script src="https://cdn.tailwindcss.com"></script>
        @if ($slots.meta)
            {{{ $slots.meta() }}}
        @endif
        @if ($slots.script)
            {{{ $slots.script() }}}
        @endif
    </head>
    <body>
        {{{ $slots.main() }}}
    </body>
</html>
